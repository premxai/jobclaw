# JobClaw - Core Dependencies
aiohttp>=3.9.0
aiohttp-socks>=0.8.0      # SOCKS4/SOCKS5/HTTP proxy support for aiohttp
curl_cffi>=0.14.0          # TLS fingerprint impersonation (Chrome/Safari/Edge) — bypasses Cloudflare/WAF
discord.py>=2.3.0
python-dateutil>=2.9.0     # Robust datetime parsing for job freshness checks
python-dotenv>=1.0.0

# Enterprise scraper (Google, Meta) - browser automation
playwright>=1.40.0

# REST API backend
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Anti-bot stealth scraping (LinkedIn/Indeed/Glassdoor)
scrapling>=0.4.0
browserforge>=1.2.0

# PostgreSQL backend (optional — SQLite is the default)
asyncpg>=0.29.0            # Async connection pool for production
psycopg2-binary>=2.9.0     # Sync PostgreSQL driver for scrapers

# AI Intelligence Layer (for embeddings, matching, dedup)
numpy>=1.26.0
sentence-transformers>=3.0.0  # Local embeddings (all-MiniLM-L6-v2)
datasketch>=1.6.0            # MinHash LSH for semantic deduplication

# Persistent worker infrastructure (Railway deployment)
arq>=0.26.0                  # Async Redis task queue (asyncio-native, 0.26+ has async run_worker)
apscheduler>=3.10.0          # In-process scheduler (replaces GitHub Actions crons)

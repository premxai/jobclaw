import AuthPageShell from "@/components/auth/AuthPageShell";

export default function SignupPage() {
    return <AuthPageShell mode="signup" title="Start your Nori board." copy="Create your account with your profile basics, then jump into your dashboard." />;
}
